# Pronoia Realty — Status & Next Steps

**Updated:** 2026-09-13
**Live:** www.pronoiarealty.com (EN) · www.pronoiarealty.com/es/ (ES)

---

## Where the project stands

The site is finished as a brochure site and fully prepared for live MLS data. Nothing in
the build is blocking. The only thing outstanding is Stellar's authorization of the feed,
which is out of our hands.

| Area | State |
|---|---|
| Site | ✅ Live, bilingual, responsive |
| Bilingual EN/ES | ✅ Two indexed URLs, hreflang, Spanish adapted for local buyers/sellers |
| Broker license in footer | ✅ C-22894, expires 8 March 2030 |
| SEO | ✅ Crawlers allowed, robots.txt + sitemap.xml, Search Console verified |
| Forms | ✅ Formspree → c.santiago@pronoiarealty.com |
| MLS pipeline (code) | ✅ Built, tested against mock data, compliant |
| MLS feed (authorization) | ❌ **Blocked on Stellar** |
| Listings shown today | Sample data, labeled "Place holder — website under development" |

---

## Scope as built

**Two real pages, not a JS language toggle.** `index.html` is the source of truth;
`es/index.html` is generated by `scripts/build-es.mjs` from `i18n/es.json` and regenerated
automatically by a GitHub Action on every push. They cannot drift apart.

**Listings are open, not gated.** A password would convert the site from IDX to VOW —
separate Stellar authorization, individual consumer registration — and would destroy the
SEO the bilingual build exists to earn. Leads are captured at the point of intent instead:
showing requests, alerts, saved searches, CMAs, off-market inventory.

**Nothing on the page claims MLS provenance while the feed is unauthorized.** The site was
opened to crawlers on 2026-09-13, so the sample properties are publicly indexable and the PR
board of REALTORS could read them. Everything that could be mistaken for real MLS content is
now suppressed while `DATA_SOURCE !== 'mlsgrid'`: the "Active" status chip, the "MLS
Verified" / "Pronoia Exclusive" / "Direct Seller" badges, the `MLS#` on each card and in the
modal, the "Homes on the market right now" heading, and the Stellar MLS credit line in the
footer. In their place: a "Place Holder" chip, a "Website under development" badge, a
prominent notice, and a modal disclaimer stating the property is not real and not sourced
from any MLS.

**This reverses itself.** None of it is a manual edit to undo — every piece is keyed to
`DATA_SOURCE`. The moment the MLS Grid token is in place and the sync writes real data, the
page returns to status chips, MLS numbers, the live heading, and the footer credit on its
own. Do not "restore" anything by hand.

**MLS compliance is built into the rendering**, not bolted on afterwards: 8-hour refresh
(rule is 12), listings dropped when they leave the feed, listing brokerage name on every
card, "Based on information submitted to the MLS GRID as of ___" with a live timestamp.
MLS remarks are never altered or translated.

**Deploys** go through commits to `main`; Render rebuilds in ~2 minutes. No tokens are
stored anywhere in the repo or in project notes.

---

## Next steps, in order

### 1. Stellar MLS feed — the only real blocker

Emailed datadelivery@stellarmls.com on 2026-09-13. Awaiting reply.

- If nothing by mid-week, **call 800-686-7451 option 4.** That queue moves faster than email.
- Press on **fee classification**: firm internal use/VOW is $450/office annually; vendor
  product is $7,500 per product annually. This is the brokerage's own IDX display, not a
  resold vendor product. Get the classification in writing before a quote is issued.
- Confirm the `OriginatingSystemName` for Stellar in Puerto Rico. The sync assumes
  `MFRMLS`. If it differs, the first run returns zero listings and looks broken when it
  isn't — fix by setting the `ORIGINATING_SYSTEM` repository variable.

**When approval lands, the remaining work is two steps:** paste the MLS Grid token into the
repo secret `MLSGRID_TOKEN`, then Actions → Sync MLS listings → Run workflow. The page
flips from the sample notice to live listings in both languages, and syncs itself every
8 hours thereafter.

### 2. Search Console housekeeping

- Add Coral as an **Owner** (Settings → Users and permissions). The property currently
  lives only in chris@marauder-ai.com. Confirm whether she uses
  `c.santiago@pronoiarealty.com` or `coral.xymar@gmail.com`.
- Check **Performance** in 1–2 weeks. The specific question: are Spanish-language queries
  appearing, and is `/es/` the page served for them? That is the direct test of whether the
  bilingual build is earning anything.

### 3. Replace the Claude project instructions

The instructions pane still holds the 2026-08-16 text. It describes a retired Zapier →
Google Drive → GitHub pipeline, predates the bilingual build, and **contains a dead GitHub
token in plain text.** Replace it with `PRONOIA-REALTY-PROJECT-CONTEXT.md`.

### 4. Smaller items, no rush

- Wire the privacy policy page.
- Test Formspree delivery end to end (submit each of the six forms, confirm arrival).
- Map view (Leaflet or Mapbox free tier) — waits on live data.
- Featured-listings section and market stats — wait on live data.
- Consider a Search Console **Domain** property later; the current URL-prefix property
  covers `https://www.pronoiarealty.com/` only.

---

## What not to undo

These were decided deliberately. Reversing them breaks something specific:

- **Filter `<option>` values stay English** even on the Spanish page — they're matched
  against `PropertyType` from the feed. Translating them silently breaks filtering.
- **The MLS Grid attribution paragraph stays in English** on both pages — the IDX rules
  specify its wording.
- **`/listings.json` and photo paths stay root-relative** — `/es/` can't resolve them
  otherwise.
- **Sync cadence stays at 8 hours or less.** 24 would breach the 12-hour refresh rule.
- **Never point automation at Stellar Central.** The feed is the only legitimate source.

---

*Detail: `docs/MLS-INTEGRATION.md` (pipeline), `docs/BILINGUAL.md` (language split),
`claude/SEO-STATUS.md` and `claude/MLS-FEED-STATUS.md` in the Claude project.*
